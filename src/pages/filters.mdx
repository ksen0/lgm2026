---
layout: ../layouts/SketchLayout.astro


slides:


  - snippet: blur
    description: <h1>Built-in Shaders</h1><p>There are preset filters available in p5.js with <code><a href=https://p5js.org/reference/p5/filter/>filter</a>()</code>. The next two examples show a user-defined shader written in GLSL and in p5.strands, and applied using <code>filter()</code>.</p><p>One of the design patterns in p5.js is to provide built-ins and defaults, which the user can optionally parameterize or extend.</p>

  - snippet: glsl
    description: <h1>Writing Shaders with GLSL</h1><p>p5.js v1 and p5.js v2 support GLSL (OpenGL Shading Language). A shader has to be defined (<code>createFilterShader(...)</code>) before usage   (<code>filter(...)</code>)</p>

  - snippet: strands
    description: <h1>Writing Shaders with p5.strands</h1><p>In p5.js 2.0+, shaders can be written using JavaScript which is then transpiled to GLSL.</p><p>A p5.strands shader has to be defined first (e.g., <code>buildFilterShader(...)</code>) and then used (e.g., <code>filter(...)</code>). In the definition, different hooks can be used to access and update data. For example, getting the current color at a pixel and changing it.</p>

  - snippet: p5strands
    description: <p>Beginner-friendliness - learners and teachers</p><p><strong>Writing p5.js shaders - GLSL and p5.strands</a></strong></p><p><a href="/lgm2026/game-of-life/">Whats next? WebGPU and compute shaders</a></p>

  - snippet: prerequisites
    description: To get started, a user does not need to know about the shader pipeline, uniforms, or varying variables. This isYou will still learn those things! But they are not required upfront like they would otherwise be; they stay out of the way until you need them. https://www.davepagurek.com/blog/p5-compute-shaders/ Regular dev chats on <a href=http://discord.p5js.org>discord.p5js.org</a>, including contributors @davepagurek @lukeplowden @perminder-17 @aashu2006 @LalitNarayanYadav @nbogie @nking07049925 @Abhayaj247 @hxrshxz @Aayushdev18 @rakesh2OO5 @avinxshKD @jjnawaaz @saurabh24thakur and others.</p>
    


  - snippet: p5strands
    description: <p>Beginner-friendliness - learners and teachers</p><p><strong>Writing p5.js shaders - GLSL and p5.strands</a></strong></p><p><a href="/lgm2026/game-of-life/">Whats next? WebGPU and compute shaders</a></p>
---
